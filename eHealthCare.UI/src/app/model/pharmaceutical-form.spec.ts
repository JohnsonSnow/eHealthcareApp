import { PharmaceuticalForm } from './pharmaceutical-form';

describe('PharmaceuticalForm', () => {
  it('should create an instance', () => {
    expect(new PharmaceuticalForm()).toBeTruthy();
  });
});
